services:
  - type: web
    name: crimex-similarity-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn similarity_service_enhanced:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
